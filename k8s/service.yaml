apiVersion: v1
kind: Service
metadata:
  name: ach-to-rtp-service
  namespace: default
  labels:
    app: ach-to-rtp-service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: ach-to-rtp-service
  sessionAffinity: None
